import Observer from '../src/observer';

fdescribe('Observer -', () => {
  let observer;

  beforeEach(() => {
    observer = new Observer();
  });

  describe('register -', () => {
    it('add channel', () => {
      expect(true).toEqual(true);
    });
  });
});
